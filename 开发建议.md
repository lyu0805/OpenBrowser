# OpenBrowser 开发建议

本文档面向参与 OpenBrowser 开发与维护的贡献者，介绍项目结构、构建与自测方式、工程约定，以及后续可改进方向。目标是让新贡献者能快速上手，并让改动保持一致的质量与跨平台可靠性。

> 面向用户的介绍见 `README.md` / `README_CN.md`。本文只谈开发。

---

## 1. 技术栈与总体设计

- **Electron 桌面应用**：主进程（`main.js`）负责窗口、IPC、系统集成；渲染进程（`renderer.js`）是本地 UI；`preload.js` 通过受信 IPC 暴露能力。
- **零运行时依赖**：`package.json` 的 `dependencies` 为空。SOCKS5/HTTP 代理桥、TLS、CDP 客户端、HTTP 服务等均为自实现。**新增功能请优先复用标准库，谨慎引入运行时依赖**——这是本项目的一项明确取舍。
- **多环境模型**：每个「环境」= 一个隔离的 Chromium Profile（独立 user-data-dir）。`engine.js` 负责其生命周期（启动、停止、看门狗、进程身份核验、锁）。
- **本地自动化**：`automation/` 是后端工具箱，通过 Local API（HTTP）/ MCP / 引擎调用，提供 RPA、代理库、应用中心、云同步、窗口同步等能力。

---

## 2. 本地构建与运行

```bash
cd Browserapp
npm start          # 启动应用（Electron）
```

跨平台注意：主浏览器窗口在 Windows 上必须 `windowsHide: false`；本地服务只绑定 `127.0.0.1`；本地文件 URL 用 `pathToFileURL` 生成（勿手写未编码的 `file:///`）。

---

## 3. 目录结构（关键部分）

```
Browserapp/
├── main.js                     # Electron 主进程、IPC、窗口
├── renderer.js                 # 本地 UI（渲染进程）
├── preload.js                  # 受信 IPC 桥
├── engine.js                   # 环境生命周期（启动/停止/看门狗/锁）
├── cdp.js                      # CDP 客户端（一次性调用 + 持久连接）
├── proxy-forwarder.js          # 本地 SOCKS5 / HTTP 代理桥
├── live-sync-v4/v5.js          # 多窗口同步
├── automation/                 # 后端工具箱
│   ├── local-api-server.js     #   本机 HTTP API（鉴权、CORS 白名单）
│   ├── mcp-server.js           #   stdio MCP 服务
│   ├── rpa-engine.js           #   RPA 步骤执行（基于 CDP）
│   ├── rpa-store.js            #   RPA 计划/任务/模板存储
│   ├── fingerprint.js          #   指纹配置生成 + 注入脚本
│   ├── isolation.js            #   数据根/Profile 路径/锁 安全校验
│   ├── platform-preflight.js   #   跨平台预检（纯函数）
│   ├── browser-kernel.js       #   内核定位与启动
│   └── protocol/               #   跨平台、窗口同步、RPA 注册表等协议层
├── assets/                     # 字体、图标等本地资源
└── *-selftest.js               # 自测脚本
```

---

## 4. 自测体系

项目以大量 `*-selftest.js` 作为回归保障（纯 Node，无需真实浏览器即可跑核心逻辑）。**提交前请至少跑与改动相关的自测**：

```bash
cd Browserapp
npm run selftest:automation   # Local API / MCP / RPA / 代理库 / 窗口同步
npm run selftest:protocol     # 协议层、跨平台进程身份、file URL 编码
npm run selftest:isolation    # 数据隔离、Profile 锁获取/释放/重开
npm run selftest:platform     # 跨平台预检（MAX_PATH / env / 沙箱 / 隔离）
npm run selftest:waitport     # 启动端口等待（事件驱动 + 轮询兜底）
npm run selftest:prepfiles    # 启动前文件准备的并行安全性
npm run selftest:templates    # RPA 模板仓库健康门禁
npm run selftest:kernel       # 内核策略
npm run selftest:startup      # 启动诊断
npm run selftest:cloud        # 云同步安全
npm run selftest:themes       # 主题接线
```

**新增功能建议同时补一个 selftest**，并在 `package.json` 注册一个 `selftest:*` 脚本。可运行的、离线的回归测试是本项目质量的主要抓手。

---

## 5. 工程约定（改动请遵循）

- **错误处理不吞异常、不留悬挂资源**：获取了锁 / 打开了服务 / 起了子进程，失败路径要成对释放。参考 `engine.js` 启动流程的资源释放写法。
- **跨平台分支要三端齐全**：涉及路径、进程、可执行文件时，`win32 / darwin / linux` 都要考虑；路径比较在 Windows 上大小写不敏感、Linux 上敏感。
- **路径安全**：用户可控路径必须做沙箱校验（见 `isolation.js` 的 `assertSafeProfileChild` / `validateProfileRootSecure`、`rpa-engine.js` 的 `resolveSafeRpaPath`），拒绝路径逃逸与符号链接逃逸。
- **本地服务安全**：HTTP 服务绑定 `127.0.0.1`、带鉴权、限制请求体大小、Origin 白名单。新增接口沿用 `local-api-server.js` 的模式。
- **指纹配置一致性**：指纹各项应稳定（同 Profile 可复现）且组合真实。修改指纹相关逻辑后跑 `selftest:isolation` 及指纹稳定性自测。
- **IPC 信任边界**：渲染进程到主进程的 IPC 一律经 `registerTrustedIpc` 校验发送方，勿绕过。

---

## 6. 后续改进方向

以下按「近期 / 中期 / 长期」组织，非强制，欢迎认领。

### 近期（低风险、见效快）

- **测试统一入口 + CI**：把分散的 `*-selftest.js` 收敛为一条命令跑全量并非零退出，接入 GitHub Actions，对 PR 做「语法检查 + 全量自测」门禁。
- **补齐关键回归用例**：为进程身份核验、启动失败后的资源释放、模板仓库健康等补测试（部分已有，可继续扩充）。
- **UI 渲染合并的推广**：渲染进程已对高频引擎事件做「每帧一次渲染 + IPC 去抖」，可继续用键控增量更新替代整表重建，减少大列表下的重排。

### 中期

- **大文件模块化**：`renderer.js` / `engine.js` / `i18n.js` / `fingerprint.js` 体量较大，建议按功能域拆分并配合构建期打包，提升可读性与可测试性。
- **CDP 连接复用**：一次性 `call()` 适合零散调用；RPA 等高频场景可复用持久连接（`PersistentConnection`）。
- **看门狗事件化**：环境存活/新标签检测目前为轮询，可评估用 `Target.*` 事件驱动，降低多开时的开销。
- **结构化日志**：将分散日志统一为分级、带环境 id 的结构化输出，便于问题定位。

### 长期

- **跨平台完善**：
  - Windows：为深层数据目录做长路径（MAX_PATH）适配；发布包代码签名。
  - macOS：正式分发采用 Developer ID 签名 + 公证 + 装订；补齐原生窗口/输入镜像以对齐 Windows。
  - Linux：完善沙箱策略与打包（AppImage/deb/rpm），处理 Wayland 下的窗口同步降级。
  - `automation/platform-preflight.js` 已把上述风险做成启动预检告警，可作为落地这些改进的路线参考。
- **性能路线**：启动关键路径可进一步把不依赖浏览器进程的准备工作并行化（需在真机分段测量后再落地，避免影响启动语言/时区等有依赖的步骤）。

---

## 7. 提交前检查清单

- [ ] 跑通与改动相关的 `npm run selftest:*`（新功能附带新 selftest 更佳）。
- [ ] 未引入新的运行时依赖（除非充分讨论）。
- [ ] 涉及路径/进程/可执行文件的改动，三端（Win/mac/Linux）都已考虑。
- [ ] 失败路径不留悬挂锁 / 服务 / 子进程。
- [ ] 面向用户/公开的文案保持中性产品表述。
- [ ] 如涉及 RPA 模板改动，`npm run selftest:templates` 通过（无收费字段 / 无未支持步骤 / 无硬编码密钥）。
